// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  fb_config: {
    apiKey: "AIzaSyD9GaS4_mg5r6YxPUJDbh4PVGAJHp3rndU",
    authDomain: "limitremoval-c46dc.firebaseapp.com",
    databaseURL: "https://limitremoval-c46dc.firebaseio.com",
    projectId: "limitremoval-c46dc",
    storageBucket: "limitremoval-c46dc.appspot.com",
    messagingSenderId: "297062079281"
  },
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
